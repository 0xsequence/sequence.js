name: Publish Dists for Packages

on:
  workflow_dispatch:
  push:
    branches:
      - dists

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package:
          - wallet-core
          - wallet-primitives
          - wallet-wdk

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: ./.github/actions/install-dependencies

      - name: Build package
        run: pnpm run build --workspace=packages/wallet/${{ matrix.package }}

      - name: Prepare dist branch
        run: |
          PACKAGE=${{ matrix.package }}
          BRANCH="dists/$PACKAGE"
          PKG_DIR="packages/wallet/$PACKAGE"

          git config user.name "github-actions"
          git config user.email "actions@github.com"

          mkdir /tmp/$PACKAGE
          cp -r $PKG_DIR/* /tmp/$PACKAGE
          cp -r $PKG_DIR/.* /tmp/$PACKAGE 2>/dev/null || true

          cd /tmp/$PACKAGE
          git init
          git checkout -b $BRANCH
          git remote add origin https://github.com/${{ github.repository }}.git
          git add .
          git commit -m "Build: publish $PACKAGE dist"
          git push -f origin HEAD:$BRANCH
