import { formatAbiParameter as m } from "./formatAbiParameter.js";
function f(e) {
  let r = "";
  const o = e.length;
  for (let t = 0; t < o; t++) {
    const a = e[t];
    r += m(a), t !== o - 1 && (r += ", ");
  }
  return r;
}
export {
  f as formatAbiParameters
};
