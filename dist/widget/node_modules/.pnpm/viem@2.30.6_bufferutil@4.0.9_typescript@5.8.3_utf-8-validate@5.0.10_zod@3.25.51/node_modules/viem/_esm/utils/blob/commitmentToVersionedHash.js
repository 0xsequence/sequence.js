import { bytesToHex as i } from "../encoding/toHex.js";
import { sha256 as m } from "../hash/sha256.js";
function y(o) {
  const { commitment: e, version: s = 1 } = o, n = o.to ?? (typeof e == "string" ? "hex" : "bytes"), t = m(e, "bytes");
  return t.set([s], 0), n === "bytes" ? t : i(t);
}
export {
  y as commitmentToVersionedHash
};
