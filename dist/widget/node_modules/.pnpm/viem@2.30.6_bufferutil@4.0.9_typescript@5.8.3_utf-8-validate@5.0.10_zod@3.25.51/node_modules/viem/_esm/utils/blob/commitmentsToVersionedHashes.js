import { commitmentToVersionedHash as i } from "./commitmentToVersionedHash.js";
function r(o) {
  const { commitments: t, version: n } = o, e = o.to ?? (typeof t[0] == "string" ? "hex" : "bytes"), s = [];
  for (const m of t)
    s.push(i({
      commitment: m,
      to: e,
      version: n
    }));
  return s;
}
export {
  r as commitmentsToVersionedHashes
};
