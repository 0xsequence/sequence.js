function a(e, f) {
  return ({ exclude: t, format: n }) => ({
    exclude: t,
    format: (r) => {
      const o = f(r);
      if (t)
        for (const m of t)
          delete o[m];
      return {
        ...o,
        ...n(r)
      };
    },
    type: e
  });
}
export {
  a as defineFormatter
};
