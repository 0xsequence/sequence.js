import { test, assert } from '../../utils/assert'

export const tests = async () => {
  test('hi', () => {
    assert.true(true)
  })
}

// TODO: hgo to town on testing everything..

// TODO: test the auth, login, etc.......

// TODO: add auth helpers to @0xsequence/auth, and heplers in "commands"
