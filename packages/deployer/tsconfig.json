{
  "extends": "../../tsconfig.json",
  "include": ["./src/**/*.ts"],
  "files": ["./hardhat.config.ts"],
  "ts-node": {
    // override compilerOptions when using ts-node
    "compilerOptions": {
      "module": "commonjs"
    }
  }
}
